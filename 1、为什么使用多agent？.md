1、为什么使用多agent？

​	单一职责原则：每个agent负责一项任务，防止多任务导致路由失败，任务执行出错；

​	上下文管理：每个agent维护各自的上下文内容，包括摘要压缩内容，近几轮对话上下文内容，预约agent多一项预约槽数据；每个agent只与当前agent维护的上下文有关，其余agent的上下文只会造成干扰；

​	防止prompt爆炸：如果每个agent都使用全局上下文，那么会出现prompt爆炸，token消耗很大，成本上升，同时系统响应变慢，用户体验不佳；

​	系统可拓展性：当需要拓展agent来实现更复杂的内容时，只需要接入分类agent，系统可拓展性提高；

​	状态易维护：将状态存储在redis中，实现无状态agent，能够避免单agent的一个状态耦合，因为任务的复杂性，可能会出现状态来回修改，导致的状态不明的情况；