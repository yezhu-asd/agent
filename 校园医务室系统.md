# 校园医务室问诊预约系统统一架构方案

## 文档信息

- 文档类型：需求说明（SRS）
- 文档状态：已正式化，可用于开发与验收
- 版本：v2.0.0-medical
- 更新时间：2026-05-29
- 适用范围：校园医务室问诊、风险识别与预约系统

---

## 1. 项目定位

本系统是一个面向校园医务室场景的智能问诊预约平台，基于多 Agent 架构构建，目标是在不改变现有技术栈的前提下，实现：

- 校园用户登录与身份识别
- 症状问诊与初步评估
- 风险识别与紧急升级
- 校医室预约与调度
- 复诊与历史记忆
- 健康知识问答
- 会话隔离与多用户并发

系统定位为：

- 校园健康咨询入口
- 症状问诊与初步评估助手
- 校医室预约与调度助手
- 复诊跟踪助手
- 健康知识问答助手

系统不替代医生，只做辅助判断和流程编排。

---

## 2. 设计原则

### 2.1 业务原则
- 只做建议，不做最终医疗结论
- 只要进入问诊流程，系统必须倾向“建议预约”或“建议进一步评估”
- 不输出“无需预约”这类绝对否定性结论
- 高风险症状优先升级
- 最终医疗判断由人工校医完成

### 2.2 工程原则
- 保持原有技术栈不变
- Agent 无状态
- 状态统一存储到 Redis / 数据库
- 同一会话串行处理
- 不同会话并行
- 结构化槽位驱动
- Prompt 尽量结构化输出 JSON

### 2.3 Token 登录与会话并发控制
- 登录后使用 UUID Token 作为会话入口
- Token 对应 user_id、phone、conversation_id
- 同一 conversation_id 内的请求必须串行处理
- 不同 conversation_id 之间可以并行处理
- 会话锁按 conversation_id 粒度控制
- 状态、历史、槽位、风险记录统一外置到 Redis / 数据库
- 推荐 Redis Key：`auth:token:{token}`、`conversation:{id}:state`、`conversation:{id}:history`、`conversation:{id}:lock`

---

## 3. 最终 Agent 架构

系统收敛为 3 个核心 Agent：

1. TaskClassificationAgent
2. DoctorAgent
3. AppointmentAgent

---

## 4. Agent 职责说明

### 4.1 TaskClassificationAgent

职责：
- 判断用户意图
- 区分是进入医生流程还是预约流程
- 处理简单闲聊
- 处理基础问答
- 判断用户是否明确表达“要预约”
- 路由请求到 DoctorAgent 或 AppointmentAgent


### 4.2 DoctorAgent

职责：
- 引导用户补充症状
- 追问症状细节
- 初步评估风险
- 判断是否需要预约
- 判断是否需要尽快就医
- 识别红旗症状
- 输出保守建议

核心原则：
- 不能输出“无需预约”
- 不能做最终排除性结论
- 默认输出“建议预约”或“建议进一步评估”
- 遇到风险症状立即升级

### 4.3 AppointmentAgent

职责：
- 抽取预约槽位
- 匹配可用时段
- 检查冲突
- 创建预约
- 改期
- 取消预约

---

## 5. 业务流程

### 5.1 登录流程

```text
学号登录
  ↓
后台获取用户信息
  ↓
生成 Token / Session
  ↓
进入问诊或预约流程
```

### 5.2 问诊流程

```text
用户输入症状
  ↓
TaskClassificationAgent 判断是否进入医生流程
  ↓
DoctorAgent 引导补充症状
  ↓
DoctorAgent 做初步评估
  ↓
若需预约 → AppointmentAgent
  ↓
返回结果
```

### 5.3 预约流程

```text
用户明确表达预约意图
  ↓
TaskClassificationAgent 判断为预约流程
  ↓
AppointmentAgent 抽取预约槽位
  ↓
检查排班与冲突
  ↓
确认预约
```

### 5.4 紧急流程

```text
识别红旗症状
  ↓
立即升级
  ↓
输出紧急提示
  ↓
建议立即就医/联系急救
```

---

## 6. 状态机设计

### 6.1 全局状态
- CLASSIFY
- DOCTOR_ASSESSMENT
- APPOINTMENT
- COMPLETED
- EMERGENCY

### 6.2 TaskClassificationAgent 状态
- CLASSIFY
- ROUTE_TO_DOCTOR
- ROUTE_TO_APPOINTMENT
- REPLY_FAQ
- GENERAL_CHAT

### 6.3 DoctorAgent 状态
- INITIAL_ASSESSMENT
- QUESTIONING
- RISK_CHECK
- RECOMMENDATION
- APPOINTMENT_NEEDED
- EMERGENCY
- DONE

### 6.4 AppointmentAgent 状态
- COLLECT_SLOT
- CHECK_AVAILABILITY
- CONFIRM_BOOKING
- DONE

---

## 7. 结构化槽位设计

### 7.1 问诊槽位
由后台用户信息 + 当前症状组成，不再要求分类 Agent 收集基础身份信息。

```json
{
  "user_id": "",
  "name": "",
  "role": "student",
  "grade_or_department": "",
  "chief_complaint": "",
  "symptom_duration": "",
  "accompanying_symptoms": [],
  "allergy_history": [],
  "past_medical_history": [],
  "fever": false,
  "trauma": false,
  "medication_taken": [],
  "red_flags": []
}
```

### 7.2 预约槽位

```json
{
  "service_type": "campus_clinic_visit",
  "preferred_date": "",
  "preferred_time_period": "",
  "urgency": "normal",
  "appointment_reason": "",
  "location": "campus_clinic"
}
```

---

## 8. 记忆系统设计

### 8.1 记忆类型
- 对话历史
- 短期记忆
- 摘要记忆
- 行为记忆

### 8.2 Redis Key 设计

| 类型     | Key                                   |
| -------- | ------------------------------------- |
| 登录态   | `auth:token:{token}`                  |
| 会话状态 | `conversation:{id}:state`             |
| 对话历史 | `conversation:{id}:history`           |
| 症状槽位 | `conversation:{id}:medical_slots`     |
| 预约槽位 | `conversation:{id}:appointment_slots` |
| 摘要记忆 | `conversation:{id}:summary_memory`    |
| 行为记忆 | `conversation:{id}:behavior_memory`   |
| 会话锁   | `conversation:{id}:lock`              |
| 风险记录 | `conversation:{id}:risk_events`       |

---

## 9. 医疗安全规则

### 9.1 不允许的输出
- 不用预约
- 你没事
- 不需要进一步处理
- 完全正常

### 9.2 允许的输出
- 建议预约
- 建议尽快预约
- 建议进一步评估
- 如症状加重请立即就医
- 建议由校医确认

### 9.3 红旗症状
一旦检测到以下情况，必须立即升级：
- 呼吸困难
- 胸痛
- 昏迷
- 抽搐
- 大出血
- 严重过敏
- 高热不退
- 严重外伤

---

## 10. 数据库设计建议

### 10.1 users
- id
- phone
- name
- role
- grade_or_department
- created_at

### 10.2 consultations
- id
- user_id
- conversation_id
- chief_complaint
- risk_level
- doctor_summary
- created_at

### 10.3 appointments
- id
- user_id
- conversation_id
- appointment_time
- status
- created_at

### 10.4 risk_events
- id
- user_id
- conversation_id
- risk_type
- description
- created_at

---

## 11. 目录结构建议

```text
campus_medical_agent/
├── agents/
│   ├── base_agent.py
│   ├── classification_agent.py
│   ├── doctor_agent.py
│   └── appointment_agent.py
├── api/
├── services/
├── db/
├── config/
├── web/
├── data/
├── tests/
├── app.py
└── requirements.txt
```

---

## 12. 请求链路

### 12.1 问诊链路

```text
用户输入
  ↓
TaskClassificationAgent
  ↓
DoctorAgent
  ↓
必要时 AppointmentAgent
  ↓
返回结果
```

### 12.2 预约链路

```text
用户明确要预约
  ↓
TaskClassificationAgent
  ↓
AppointmentAgent
  ↓
返回预约结果
```

---

## 13. 最终统一结论

### TaskClassificationAgent
- 只做分类、路由、闲聊、基础问答
- 不收集基础身份信息
- 不引导症状

### DoctorAgent
- 负责症状补充、追问、风险判断
- 决定是否建议预约
- 不能输出无需预约
- 默认保守地建议预约或进一步评估

### AppointmentAgent
- 负责所有预约相关动作
- 处理明确预约意图
- 完成预约、改期、取消

---

## 14. 技术选型

- FastAPI
- Jinja2
- HTML / CSS / JS
- Redis
- MySQL
- Azure OpenAI
- Pinecone
- 自定义 Orchestrator
- logging
- 环境变量 + Settings

---

## 15. 总结

本系统在技术栈保持不变的前提下，将原有心理咨询室预约 agent 迁移为校园医务室问诊预约系统，通过合并 Agent 职责、收敛流程、强化医疗安全规则，形成一套更适合校园场景的轻量级多 Agent 医疗辅助平台。

---

## 16. Section 2.4 - 数据库层医学化（Database Models）

### 16.1 核心表结构变更

#### 医生表（Doctor）- 替代 Technician

```
doctors 表（原 technicians）
├── id (PK)
├── name (UNIQUE) - 医生姓名
├── gender - 医生性别
├── specialty** - 专科（内科、外科、皮肤科等）
├── license_number (UNIQUE)** - 执业证号
├── education - 教育背景（博士、硕士等）
├── years_of_experience - 从业年数
├── consultation_rating - 问诊评分（1-5）
├── max_daily_appointments - 每天最多预约数
└── 关系: schedules (1:N DoctorSchedule)
```

#### 医生值班表（DoctorSchedule）- 替代 TechnicianSchedule

```
doctor_schedules 表（原 technician_schedules）
├── id (PK)
├── doctor_id (FK) - 外键关联 doctors
├── start_time
├── end_time
├── status - 'busy', 'free', 'on_leave'
├── shift_type** - 班次类型：'morning', 'afternoon', 'evening'
└── appointment_id
```

#### 问诊记录表（ConsultationRecord）- 替代 UserBehavior

```
consultation_records 表（原 user_behaviors）
├── id (PK)
├── user_id - 用户ID
├── doctor_id (FK) - 问诊医生
├── consultation_type** - 'initial', 'follow_up', 'emergency'
├── symptoms** - JSON 症状列表
├── diagnosis** - 诊断信息
├── prescription - JSON 处方信息
├── risk_level** - 'low', 'medium', 'high'
├── resolved - 是否已解决
├── conversation_id
├── session_id
├── created_at
└── resolved_at
```

#### 医学偏好表（MedicalPreference）- 替代 UserPreference

```
medical_preferences 表（原 user_preferences）
├── id (PK)
├── user_id
├── preference_type - 'doctor', 'appointment_time', 'consultation_type'
├── preference_value
├── confidence_score - 偏好置信度
└── last_updated
```

#### 健康建议表（HealthRecommendation）- 替代 UserRecommendation

```
health_recommendations 表（原 user_recommendations）
├── id (PK)
├── user_id
├── recommendation_type - 'follow_up', 'health_tips', 'appointment_reminder', 'urgent_care'
├── content
├── doctor_id (FK)
├── priority - 'low', 'normal', 'urgent'
├── is_sent
├── created_at
└── sent_at
```

#### 风险事件表（RiskEvent）- 新增表

```
risk_events 表（新增）
├── id (PK)
├── user_id
├── risk_type - 'red_flag_symptom', 'frequent_consultation', 'chronic_condition'
├── description
├── risk_score - 0-100
├── detected_at
└── resolved_at
```

### 16.2 Repository 层重构

#### DoctorRepository（替代 TechnicianRepository）

**主要方法：**
- `add_doctor()` - 添加医生
- `get_doctor_by_id()` - 按ID获取
- `get_doctors_by_specialty()` - 按专科查询
- `get_top_rated_doctors()` - 获取评分最高的医生
- `update_doctor_rating()` - 更新评分
- `add_schedule()` - 添加排班
- `get_available_doctors_at_time()` - 查询指定时间可用医生

**向后兼容：**
- `TechnicianRepository` 继承 `DoctorRepository`
- 提供 `add_technician()`, `get_technician_by_id()` 别名

#### ConsultationRepository（替代 UserBehaviorRepository）

**主要方法：**
- `record_consultation()` - 记录问诊
- `get_user_consultation_history()` - 获取问诊历史
- `get_recurring_symptoms()` - 获取反复出现的症状
- `mark_consultation_resolved()` - 标记为已解决
- `get_user_health_profile()` - 获取用户健康档案
- `get_consultation_statistics()` - 统计统计

**医学偏好方法：**
- `update_medical_preference()`
- `get_medical_preferences()`
- `get_top_preferences()`

**健康建议方法：**
- `create_health_recommendation()`
- `get_pending_recommendations()`
- `mark_recommendation_sent()`

**风险事件方法：**
- `record_risk_event()` - 记录风险事件
- `get_active_risk_events()` - 获取活跃风险
- `resolve_risk_event()` - 解决风险事件

### 16.3 向后兼容性设计

**在 db/models.py 中添加别名：**
```python
# 向后兼容
Technician = Doctor
TechnicianSchedule = DoctorSchedule
UserBehavior = ConsultationRecord
UserPreference = MedicalPreference
UserRecommendation = HealthRecommendation
```

**在 db/repositories/__init__.py 中导出：**
```python
from .doctor_repository import DoctorRepository, TechnicianRepository
from .consultation_repository import ConsultationRepository
from .user_behavior_repository import UserBehaviorRepository

__all__ = [
    'DoctorRepository',
    'TechnicianRepository',  # 向后兼容别名
    'ConsultationRepository',
    'UserBehaviorRepository'  # 向后兼容别名
]
```

### 16.4 文件更新清单

✅ **db/models.py** - 完全重构
  - Doctor 表（8 个医学字段）
  - DoctorSchedule 表（班次类型字段）
  - ConsultationRecord 表（症状、诊断、处方、风险等级）
  - MedicalPreference、HealthRecommendation、RiskEvent 表
  - 向后兼容别名

✅ **db/base/interfaces.py** - 更新接口
  - BaseDoctorRepository（替代 BaseTechnicianRepository）
  - BaseScheduleRepository（更新为医学语义）

✅ **db/repositories/doctor_repository.py** - 新建
  - 400+ 行完整医生数据访问代码
  - 支持按专科、评分等医学查询
  - TechnicianRepository 继承以兼容

✅ **db/repositories/consultation_repository.py** - 新建
  - 500+ 行完整问诊和行为分析代码
  - 医学偏好、健康建议、风险事件管理
  - 完整的统计分析方法

✅ **db/repositories/__init__.py** - 更新导入
  - 导出新的医学化 Repository
  - 保留向后兼容别名

### 16.5 编译验证结果

```
✅ db/models.py - 编译成功
✅ db/base/interfaces.py - 编译成功
✅ db/repositories/__init__.py - 编译成功
✅ db/repositories/doctor_repository.py - 编译成功
✅ db/repositories/consultation_repository.py - 编译成功
✅ db/repositories/knowledge_repository.py - 编译成功
```

**应用启动状态：** ✅ 运行中  
**健康检查：** ✅ 200 OK  
**API 端点：** ✅ 所有 7 个医学端点已注册

---

问题1、现在agent是无状态的嘛？每一个用户都要创建对应的四个agent嘛？那如果上千个用户同时使用？是不是实例过多？
问题2：现在状态时存在redis嘛？什么数据结构？如果是hash，key和value分别是什么？每次对话临时从redis获取嘛？
问题3：现在会话内串行，那是加锁还是说像豆包一样，有个按钮，用户点了按钮才可以暂停流程，继续输入？